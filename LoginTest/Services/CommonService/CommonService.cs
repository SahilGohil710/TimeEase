using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace LoginTest.Services.CommonService
{
    public class CommonService
    {
        //public bool IsLogin()
        //{
        //    var email = User.Identity.Name;
        //    var LoginAuth = _repository.M_Login.Where(m => m.SessionId.Trim() == identity.User.SessionId.Trim() && m.AD_ID.Trim() == identity.User.UserADID.Trim() && m.LoggedIn == false).FirstOrDefault();
        //    if (LoginAuth != null)
        //    {
        //        return false;
        //    }
        //    return true;
        //}
    }
}